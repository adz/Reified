name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore

      - name: Run test harness
        run: dotnet run --project tests/EffectfulFlow.Tests/EffectfulFlow.Tests.fsproj --nologo

      - name: Run main example
        run: dotnet run --project examples/EffectfulFlow.Examples/EffectfulFlow.Examples.fsproj --nologo

      - name: Run maintenance example
        run: dotnet run --project examples/EffectfulFlow.MaintenanceExamples/EffectfulFlow.MaintenanceExamples.fsproj --nologo

      - name: Publish and run NativeAOT probe
        run: bash scripts/run-aot-probe.sh
