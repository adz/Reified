# Reified 0.5.0

## JSON codecs

- Integer encoding writes directly to the UTF-8 buffer instead of allocating a temporary array per value. The local
  benchmark suite now covers a mixed aggregate, a 24-field record, and integer lists up to 10,000 items.
