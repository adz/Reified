# Axial documentation

Axial contains two independent F# products. Load only the context for the product being used:

- Schema, Data, ErrorHandling, parsing, validation, refined values, codecs, and contracts: `/schema/llms.txt`
- Flow, effects, services, resources, concurrency, and hosting: `/flow/llms.txt`

Product documentation starts at `/schema/` and `/flow/`.
