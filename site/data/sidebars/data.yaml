groups:
  - title: Axial.Data
    home: /data
    kind: primary
    caption: Portable structured values for documents, fixtures, boundaries, and adapters.
    children_of:
      - /data
    exclude:
      - /data/reference
    reference:
      children_of:
        - /data/reference
