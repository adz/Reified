groups:
  - title: Axial.ErrorHandling
    home: /error-handling
    kind: primary
    caption: Installs Result, Check, Parse, and Refined together.
    descendants: false
    entries:
      - /error-handling/getting-started
      - /error-handling/reference-app
      - /error-handling/fstoolkit-comparison

  - title: Axial.Result
    home: /error-handling/result
    caption: Fail-fast Result helpers and the result computation expression.
    descendants: false
    entries:
      - /error-handling/tutorials
      - /error-handling/result-builder
    reference:
      children_of:
        - /error-handling/reference/result

  - title: Axial.Check
    home: /error-handling/check
    caption: Reusable, named structural checks that attach to Result.
    children_of:
      - /error-handling/check
    reference:
      children_of:
        - /error-handling/reference/check
      entries:
        - /error-handling/reference/predicate

  - title: Axial.Parse
    home: /error-handling/parse
    caption: Named parsers for serialized primitive values.
    descendants: false
    reference:
      children_of:
        - /error-handling/reference/parse

  - title: Axial.Refined
    home: /error-handling/refined
    caption: Construct values whose types record successful checks.
    children_of:
      - /error-handling/refined
    reference:
      children_of:
        - /error-handling/reference/refined
