# Sidebar for the repository-wide getting started page and its siblings. The four product areas
# are top-nav items, so this sidebar lists only the getting-started pages; routing onward to the
# areas lives on the landing page and on /where-to-go-next/.
groups:
  - title: Getting started
    home: /getting-started
    kind: primary
    caption: Declare one model, parse untrusted input into it, and derive a JSON codec from the same declaration.
    descendants: false
    entries:
      - /what-a-schema-gives-you
      - /where-to-go-next
