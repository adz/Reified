---
title: API Reference
description: Landing page for the FsFlow API reference.
---

# API Reference

This page shows the API reference by package first, then by user-interest surface.

The detailed package hubs below are the first stop. The member pages sit underneath them.

## Package split

The reference is split the same way the code is split:

- `FsFlow` for synchronous and async boundaries, validation helpers, and support types
- `FsFlow.Net` for task-oriented boundaries, cold task helpers, and task-native interop

## Read this first

Choose the package hub that matches the runtime shape of the code you are writing, then drill into the landing pages beneath it.

<div class="docs-grid">

<section class="docs-card">
<span class="label">FsFlow</span>
<h2><a href="fsflow/">FsFlow hub</a></h2>
<p>Flow, AsyncFlow, Validate, and the support types used by the core package.</p>
</section>

<section class="docs-card">
<span class="label">FsFlow.Net</span>
<h2><a href="fsflow-net/">FsFlow.Net hub</a></h2>
<p>TaskFlow, ColdTask, and the task-oriented interop surface.</p>
</section>

</div>

<div class="docs-stack">

<section class="docs-card">
<span class="label">Open first</span>
<h3><a href="fsflow/">FsFlow</a></h3>
<p>Start with the core package hub, then move into the Flow, AsyncFlow, Validate, and support-type pages.</p>
</section>

<section class="docs-card">
<span class="label">Open first</span>
<h3><a href="fsflow-net/">FsFlow.Net</a></h3>
<p>Start with the task package hub, then move into the TaskFlow, ColdTask, and interop pages.</p>
</section>

</div>
